server:
  port: 8082

spring:
  application:
    name: service-payment
  datasource:
    url: jdbc:postgresql://${DB_HOST:postgres-payment}:5432/${DB_NAME:paymentdb}
    username: ${DB_USER:payment_user}
    password: ${DB_PASS:payment_pass}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: false
  rabbitmq:
    host: ${RABBITMQ_HOST:rabbitmq}
    port: 5672
    username: ${RABBITMQ_USER:guest}
    password: ${RABBITMQ_PASS:guest}
    listener:
      simple:
        # Retry on transient errors (e.g. DB not ready yet)
        retry:
          enabled: true
          initial-interval: 1000ms
          max-interval: 10000ms
          multiplier: 2.0
          max-attempts: 3
        default-requeue-rejected: false   # don't loop poison pills

payment:
  fail-threshold: ${PAYMENT_FAIL_THRESHOLD:500.00}
  max-amount: ${PAYMENT_MAX_AMOUNT:10000.00}

loki:
  url: ${LOKI_URL:http://loki:3100}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  metrics:
    tags:
      application: ${spring.application.name}
  tracing:
    sampling:
      probability: 1.0

logging:
  pattern:
    level: "%5p [${spring.application.name},%X{traceId:-},%X{spanId:-}]"

otel:
  exporter:
    otlp:
      endpoint: ${OTEL_ENDPOINT:http://tempo:4317}
app:
  jwt:
    secret: shopflow-super-secret-key-change-in-production-must-be-256-bits-long

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${server.port}