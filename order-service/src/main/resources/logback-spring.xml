<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <springProperty scope="context" name="appName" source="spring.application.name" defaultValue="service-order"/>
    <springProperty scope="context" name="lokiUrl" source="loki.url" defaultValue="http://loki:3100"/>

    <!-- Console appender with structured-ish pattern -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %highlight(%-5level) [${appName}] [%X{traceId:-no-trace},%X{spanId:-no-span}] %cyan(%logger{36}) - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Loki appender for Grafana -->
    <appender name="LOKI" class="com.github.loki4j.logback.Loki4jAppender">
        <http>
            <url>${lokiUrl}/loki/api/v1/push</url>
        </http>
        <format>
            <label>
                <pattern>app=${appName},level=%level,host=${HOSTNAME:-local}</pattern>
            </label>
            <message>
                <pattern>level=%level traceId=%X{traceId:-none} spanId=%X{spanId:-none} logger=%logger{36} thread=%thread msg="%msg"</pattern>
            </message>
            <sortByTime>true</sortByTime>
        </format>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="LOKI"/>
    </root>

    <!-- Reduce noise from framework internals -->
    <logger name="org.springframework.amqp" level="WARN"/>
    <logger name="org.hibernate.SQL" level="WARN"/>
    <logger name="com.shopflow" level="DEBUG"/>

</configuration>
